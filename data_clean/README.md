# Cleaned data

This folder contains cleaned, analysis-ready datasets generated from `data_raw/`.

The main cleaned soda-tax panel should be saved here, for example:

- `soda_tax_panel_clean.csv`

Cleaned data should be generated by:

```r
code/01_clean_soda_tax_data.R
